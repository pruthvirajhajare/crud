import { SerachpipePipe } from './serachpipe.pipe';

describe('SerachpipePipe', () => {
  it('create an instance', () => {
    const pipe = new SerachpipePipe();
    expect(pipe).toBeTruthy();
  });
});
